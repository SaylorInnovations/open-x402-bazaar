# Example: a reference pointer

Template — replace with a real reference once one exists. This is a pointer,
not a fact: where to look, not what's there right now (since that changes and
this file won't be updated every time it does).

Example: "Bugs are tracked in Linear project INGEST — check there for context
on pipeline-related issues," or "The oncall dashboard is at
grafana.internal/d/api-latency — check it before touching request-path code."

Delete this file once a real reference replaces it.
